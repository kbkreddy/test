
UserConnected = function(user)   -- User conneted to the hub	
	--core.Disconnect(user.sNick, "<bye from lua> dudiluu|")  -- disconnects user from hub with reason message
            --core.Disconnect(user.sNick) -- disconnects user from hub;
end

print("loaded Bye.lua")          -- shows message in debug Feed